name: SolMate MQTT Integration
version: "1.0"
slug: solmate_mqtt
description: "A custom add-on to integrate SolMate data via MQTT."
startup: application
boot: auto
ingress: false
options: {}
schema: {}

# The Docker image will be built using the local Dockerfile.
image: local/solmate_mqtt
